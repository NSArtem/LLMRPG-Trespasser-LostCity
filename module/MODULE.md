# lair of the lamb 17

- Module ID: `module-lair-of-the-lamb`
- Verification: `verified`
- Source file: `Module - Lair of the Lamb.pdf`

## Runtime loading

Use `index.md` or `index.json` to locate a relevant card, then load only that card and the cards named in its references.

`audit/` is extraction evidence and is not gameplay context. Do not load it during normal play.

On place cards, only `First impression` is player-safe. The other sections are GM context.
